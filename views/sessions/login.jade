extends ../layouts/layout

block content
  ul.login-choices
    each service in ['instagram', 'facebook', 'twitter', 'g+']
      li
        a(href='/login/'+ service class='btn btn-hg btn-embossed btn-primary btn-' + service)
          | Login with #{service}
